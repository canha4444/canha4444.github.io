<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{pageTitle}
        block styles
    body
        header.main-header
            nav.main-header__nav
                ul.main-header__item-list
                    a(href='/')
                     img(src='/image/download.png')
                    li.main-header__item
                        a(href='/',class=(path === '/' ? 'active' : '')) SHOP
                    li.main-header__item
                        a(href="/admin/add-product",class=(path === '/admin/add-product' ? 'active' : '')) NEWS
                    li.main-header__item
                        a(href='#') CHARACTERS
                    li.main-header__item
                        a(href='#') MANGA
                    li.main-header__item
                        a(href='#') FORUMS
        block content